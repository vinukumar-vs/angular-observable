import { AddToGroupDirective } from './add-to-group.directive';

describe('AddToGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new AddToGroupDirective();
    expect(directive).toBeTruthy();
  });
});
